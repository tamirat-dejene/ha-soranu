apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api-gateway
  template:
    metadata:
      labels:
        app: api-gateway
    spec:
      containers:
      - name: api-gateway
        image: ha-soranu/api-gateway
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
          requests:
            memory: "64Mi"
            cpu: "250m"
        ports:
        - containerPort: 3030
        env:
        - name: GATEWAY_HTTP_ADDR
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: GATEWAY_HTTP_ADDR
        
        - name: GATEWAY_GRPC_ADDR
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: GATEWAY_GRPC_ADDR

        - name: KAFKA_BROKERS
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: KAFKA_BROKERS

---
apiVersion: v1
kind: Service
metadata:
  name: api-gateway
spec:
  type: LoadBalancer
  ports:
  - protocol: TCP
    port: 3030
    targetPort: 3030
  
  selector:
    app: api-gateway