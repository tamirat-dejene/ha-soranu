syntax = "proto3";

package test;

option go_package = "/testpb;testpb";

message TestMessage {
  string id = 1;
  string content = 2;
}
service TestService {
  rpc TestMethod(TestMessage) returns (TestMessage);
}