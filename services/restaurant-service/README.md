# Restaurant-Service Service

This is the **restaurant-service microservice**, generated using a Clean Architecture skeleton.

## Structure

- `cmd/` – service entrypoint (gRPC)
- `internal/` – API handlers, usecases, repositories
- `migrations/` – SQL migrations + runner
- `docs/` – documentation
- `test/` – tests

## Commands

```bash
# Customize run/build targets for your service
make test
```
